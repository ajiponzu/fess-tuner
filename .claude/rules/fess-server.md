# Fess サーバー環境

## 接続情報

- サーバーURL: `http://piserver:8080`
- 実行環境: Docker コンテナ

## データパス

- クロール対象データの配置先: `/var/www`（Fess コンテナ内から見たパス）
- ホスト側のパスとのマッピングはホスト環境に依存する

## 注意事項

- クロールを実行するまでインデックスは空の状態である
- データを `/var/www` に配置した後、`fess-crawl` でクロールをトリガーする必要がある
